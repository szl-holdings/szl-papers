name: Doctrine Overclaim Guard

# Grep-based honesty gate. The rule now lives in ONE place — the org-shared
# reusable workflow in szl-holdings/.github — so every repo that publishes
# Λ-uniqueness / Conjecture-1 claims inherits the same check automatically.
# Fails CI if a governed surface claims unconditional Λ uniqueness. Conjecture 1
# is disproved as stated; only conditional Theorem U and the weaker-condition
# open question may be reported.
on:
  workflow_dispatch: {}
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  overclaim:
    uses: szl-holdings/.github/.github/workflows/reusable-overclaim-guard.yml@464069d945b10e51893fc749f5530b7643f23ad4
